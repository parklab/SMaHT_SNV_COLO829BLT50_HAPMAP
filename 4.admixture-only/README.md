# Admixture-Only Variant Identification and Visualization
The admixture only variants were broadly identified by intersecting the RUFUS and Mutect2 calls for the COLOBLT50 cell line with the truth set found in this publication. These data sets served as the inputs to all visualizations generated in Figures 3 and Figures S4.

## Creating admixture only vcfs with the union of RUFUS and Mutect2 calls
1. The RUFUS variant calling algorithm was called using low-frequency detection parameters in 1mb mode using a paired contrl. Each run utilized a Genome Characterization Center (GCC) -specific sequencing bam (e.g. BCM, Broad, NYGC, UW, WashU) as the subject file, and the COLO829 BL-only bam (i.e. `COLO829BL_Ill_230X.bam`) for the paired control file. The full script to launch all jobs can be found in `launch_split_runs.sh`, but an example region call looked like:
```bash
bash runRufus.sh -s $SUBJECT -c $CONTROL -r $REFERENCE -f ${RESOURCE_DIR}GRCh38_full_analysis_set_plus_decoy_hla.25.Jhash -m 5 -k 25 -t 40 -L -vs -R chr${curr_chr}:${start_coord}-${end_coord}
```

2. Each region vcf was then trimmed and combined using `trim_and_combine.sh`, and normalized using `post_process.sh`. These vcfs are found in the `rufus_vcfs` directory.
3. Mutect2 vcfs, called in a similar fashion of one GCC vs the COLO829 BL-only paired control, were procured from the Data Acquisition Center (DAC) and Peter Park's group at Harvard Medical School. These vcfs are found in the `mutect2_vcfs` directory.
4. Each Mutect2 and RUFUS vcfs specific to a GCC were intersected (e.g. `mutect2_vcfs/NORMED.PASS_SNPs_ONLY.COLO829BLT-BCM.vcf.gz` with `rufus_vcfs/sorted.atomed.normed.bcm.400x.merged.SMHTCOLO829BLT50.FINAL.no_inherited.SNV_only.hdaf_single_sample.vcf.gz`)
5. The unions of these intersection was concatenated into a single vcf.
6. The union vcfs were then intersected with bitmasks representing the possible degrees of intersection for the five GCC-sets (i.e. 1-5), and intersected with the DAC-published truth set of the COLOBLT50 mixture to retain the complement (i.e. those **not** identified in the truth set - `SMaHT_COLO829_SNV_truth_set_v1.0.vcf.gz`). This created five vcfs - one containing admixture-only variants that were found in a single GCC data set (i.e. BCM or Broad or NYGC or UW or WashU), one containing admixture-only variants found in two GCCs (i.e BCM and Broad or BCM and NYGC...), ... up to the vcf with admixture-only variants found in all five GCCs.
7. These vcfs from step 6 served as the input to the next visualization steps detailed below and are referred to as `admixture-only vcfs`, and the five intersection sets as `admixture-only categories`  The code describing steps 4-6 can be found in `mut_ruf_isec_all.sh`
TODO: put in separation of snv/indel

## Figure 3 Generation
### The following procedures were done separately on vcfs containing SNVs, and vcfs containing INDELs

### Upset Plot
1. Mutect2 and RUFUS vcfs per GCC, plus the DAC-generated truth-set, were fed into `mut_ruf_isec_all.sh` to create all possible intersections of the call-sets that are not contained in the truth-set. 
2. The output from step 1 gets saved into a file `upset_counts.txt`. These counts were copied and pasted into the visualization script `upset_plot.ipynb` for Fig. 3C generation.

## Allele Frequency Histogram Plot
1. To calculate allele frequencies in a caller independent fashion, pileups were performed at each site documented in the vcfs created in Step 1 of the Upset Plot protocol. The pileups were performed on COLOBLT50 long read bam files (generated by PacBio). 
2. Pileup vcfs respective to individual bam files were given unique identifiers with `reheader.sh` to avoid merge conflicts from bam files representing the same sample (i.e. technical replicates). 
3. After ensuring unique subject IDs, all pileup vcfs were merged together using `merge_pileups_vcfs.sh` 
4. For each vcf, the alternate observations `AO` specific to the allele in question were divided by the depth `DP`, and annotated as `INFO/ALLELIC_PCT` (this procedure done in `add_total_depth_info_field.sh`).
5. Variants per admixture-only category, were then binned by allele frequency (annotated as `INFO/ALLELIC_PCT`), using `batch_filter_af.sh`
6. Finally, counts were tallied using `composed_counts.sh` and transformed into arrays for input to `af_histo.ipynb`. The python script was then run to generate Fig. 3D.
#TODO: add reheader.sh, merge_pileup_vcfs.sh, add_total_depth_info_field.sh to directory
#TODO: update viz code with correct counts

## Supplemental Figure 4 Generation
### SNVs
1. To validate SNVs, we performed pileups in all available COLOBLT50 long read bam files (generated by PacBio). The sites for the pileups were the five admixture-only vcfs (generated as described in the top section of this README). 
2. As a negative control, we peformed pileups in the same sites as step one, but in a completely unrelated control, sequenced by the same GCCs and using the same technologies.
3. Each pileup corresponding to a single category were them merged into a single file (e.g. all 1-way BLT50 pileups, all 2-way BLT50 pileups... all 1-way negative control pileups, all 2-way negative control pileups) to produce 10 total vcfs. The total variant counts for each of these vcfs correspond to the lighter yellow and purple bars in panels a-e.
4. Each of the ten depth-filtered vcfs were then filtered to extract only ALT alleles found in the corresponding admixture-only vcf (i.e. 1-way BLT50 vcf and 1-way negative control vcf were intersected with the 1-way admixture-only vcf).
5. The ALT vcfs were then filtered by the AO field at increasing intervals - 1, 2, 5, 8, and 10. These counts represent the darker orange and purple bars found in panels a-e.
6. The final filtered vcf counts were tallied to a plain text file, counts.out. All of this was performed in `merge_and_tally.sh` `transpose_counts.sh` was then run on the counts.out file to make copying the counts easily into the final python visualization script
4. `s4_double_histo.ipynb` and `s4_line_chart.ipynb` were then run with these counts to provide the histogram visualizations and line chart visualization seen in Figure S4.

### INDELs
Due to the high rate of error in PacBio sequencing in homopolymeric regions, we employed a different strategy to validate insertions and deletions.

1. 
